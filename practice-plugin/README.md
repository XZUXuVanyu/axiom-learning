# Learner implementation workspace

This directory will hold the evolving practice plugin. No implementation or package setup is supplied yet.

Start from lessons/L001-runtime-validation.md. Confirm the local environment before choosing build/run commands. The learner will create the crucial TypeScript/C++ implementation; administrative setup can be guided by the tutor.
