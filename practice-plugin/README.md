# Learner implementation workspace

The active path is C++: start with lessons/C001-tool-contract.md and progress.md.

The learner creates the crucial feature logic, validation, result handling, and relevant checks. Administrative build setup may be guided after inspecting the local compiler/IDE. No implementation or build configuration is supplied in this update.

Keep the core capability independent of MCP transport so it can be exercised locally. Select layout and libraries when the actual feature needs them; do not begin with a general plugin framework. Existing external TypeScript work remains the learner's historical work and need not be migrated to start C001.
